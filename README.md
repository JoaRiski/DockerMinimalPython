# Docker Minimal Python

This repository is an exmaple of how to build a minimal python container by
building the python code into a statically linked binary and adding that to
the scratch container.
